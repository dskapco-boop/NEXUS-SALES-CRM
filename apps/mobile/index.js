// Expo Router entry point
// This file registers the app with the Expo Router
// The package.json "main": "expo-router/entry" handles the routing
// but we need this file as a fallback for explicit entry resolution
import 'expo-router/entry';
